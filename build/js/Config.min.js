function download(e,t){(element=document.createElement("a")).setAttribute("href","data:text/plain;charset=utf-8,"+encodeURIComponent(t)),element.setAttribute("download",e),element.style.display="none",document.body.appendChild(element),element.click(),document.body.removeChild(element)}Elements=["alert","dropdown","line","tooltip","blockquote","breadcrumb","button","navbar","pagi","pg-bar","table","tabs"],Elements_Used=[],Elements.forEach(function(e){element_use=document.querySelector(e),null!=typeof e&&Elements_Used.push(e)}),Elements_Used_text=`"${Elements_Used[0]}"`,Elements_Used.splice(0,1),Elements_Used.forEach(function(e){return Elements_Used_text+=`, "${e}"`}),config=document.querySelector("config"),jsonexport=`
{
  "Used": [
      ${Elements_Used_text}
  ],
  "Utilities": [
      ${config.getAttribute("Utilities")}
  ],
  "Systems": {
      ${config.getAttribute("Systems")}
  },
  "FormUse": ${config.getAttribute("FormUse")},
  "dist": "${config.getAttribute("dist")}"
}
`,"true"==config.getAttribute("download")&&download("godui.json",jsonexport);